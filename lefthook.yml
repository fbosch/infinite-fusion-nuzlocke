output:
  - failure
  - summary

pre-commit:
  commands:
    lint-staged:
      run: pnpm exec lint-staged

pre-push:
  commands:
    badges:
      run: pnpm badges:staged {push_files}
    quality:
      run: pnpm quality:pre-push
