output:
  - failure
  - summary

pre-commit:
  commands:
    lint-staged:
      run: pnpm exec lint-staged
    badges:
      run: pnpm badges:staged {staged_files}

pre-push:
  commands:
    quality:
      run: pnpm quality:pre-push
